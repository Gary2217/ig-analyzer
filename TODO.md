# TODO (next session)

- DB schema uses snapshot_date, not day
- ig_daily_snapshots lacks reach / interactions columns
- daily-snapshot API always falls back
- Decide:
  A) Adapt API to existing schema
  B) Proper migration + backfill + cron rewrite
